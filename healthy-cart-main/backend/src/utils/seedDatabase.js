import { Product } from "../models/Product.js";import { SiteContent } from "../models/SiteContent.js";import { seedProducts } from "../data/seedProducts.js";import { seedSiteContent } from "../data/seedSiteContent.js";export async function seedDatabase() {  const existingProducts = await Product.find({}, { slug: 1 }).lean();  const existingSlugs = new Set(existingProducts.map((product) => product.slug));  const missingProducts = seedProducts.filter((product) => !existingSlugs.has(product.slug));  if (missingProducts.length > 0) {    await Product.insertMany(missingProducts, { ordered: false });  }  const siteContent = await SiteContent.findOne({ key: seedSiteContent.key }).lean();  if (!siteContent) {    await SiteContent.create(seedSiteContent);  }}